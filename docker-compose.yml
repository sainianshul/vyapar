# ============================================================
# VCanCare — Docker Compose (FrankenPHP + Octane + Redis)
# ============================================================

services:

  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: vvyapar_mitra_app
    restart: unless-stopped
    working_dir: /var/www
    ports:
      - "8101:8000"
    volumes:
      - ./:/var/www
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_started
    environment:
      - OCTANE_SERVER=frankenphp

  # Queue Worker — processes jobs from Redis
  queue:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: vvyapar_mitra_queue
    restart: unless-stopped
    working_dir: /var/www
    volumes:
      - ./:/var/www
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_started
    command: ["php", "artisan", "queue:work", "redis", "--sleep=3", "--tries=3", "--max-time=3600"]

  mysql:
    image: mysql:8.0
    container_name: vvyapar_mitra_mysql
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: laravel
      MYSQL_ROOT_PASSWORD: root
      MYSQL_PASSWORD: root
      MYSQL_USER: laravel
    ports:
      - "3307:3306"
    volumes:
      - vvyapar_mitra_mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5

  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    container_name: vvyapar_mitra_phpmyadmin
    restart: unless-stopped
    ports:
      - "8081:80"
    environment:
      PMA_HOST: mysql
      MYSQL_ROOT_PASSWORD: root
    depends_on:
      - mysql

  redis:
    image: redis:7-alpine
    container_name: vvyapar_mitra_redis
    restart: unless-stopped
    ports:
      - "6380:6379"
    volumes:
      - vvyapar_mitra_redis_data:/data
    command: redis-server --appendonly yes --maxmemory 256mb --maxmemory-policy allkeys-lru

volumes:
  vvyapar_mitra_mysql_data:
  vvyapar_mitra_redis_data: